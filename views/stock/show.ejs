<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>
</head>
<body>
    <h1><%=stocks.name%></h1>
    <p><b>$<%=stocks.price%></b><p>
    <p><b>Category:<%=stocks.category%></b></p>
    <footer>
        <form action="/stock/<%=stocks.id%>/buy" method="POST">
            <div>
                <label for="buy">Buy</label>
                <input type="number" id="buy" name="amount" value="amount" max=<%=Math.floor(findUser.cash/stocks.price)%>>
            </div>
            <button>Add Position</button>
        </form>
        <form action="/stock/<%=stocks.id%>/sell" method="POST">
            <div>
                <%let x=0;%>
                <%for(let i=0; i<findUser.stocks.length;i++){%>
                    <%if(findUser.stocks[i].stock.equals(stocks.id)){%>
                        <%x = findUser.stocks[i].amount%>
                    <%}%>
                <%}%>
                <label for="sell">Sell</label>
                <input type="number" id="sell" name="amount" max="<%=x%>">
            </div>
            <button>Remove Position</button>
        </form>
    <form action="<%=stocks.id%>/watchlist" method="POST">
       <button>Add to Watchlist</button>
    </form>
        <button><a href="/">Home</a></button>
    </footer>
</body>

</html>